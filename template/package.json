{
  "name": "faster-auto-app",
  "version": "0.0.1",
  "description": "",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "rm -rf dist/* && tsc",
    "dev": "cross-env NODE_ENV=development tsnd --respawn -r tsconfig-paths/register  ./src/index.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.14.6",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "faster-auto": "^0.0.1"
  }
}
